{
  "name": "dev",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "abreact": "file:../command",
    "prettier": "^1.16.4"
  },
  "scripts": {
    "build": "npm start --prefix ../command -- build --path $(pwd)",
    "start": "npm start --prefix ../command -- start --path $(pwd)"
  }
}
